{
  "name": "ts-dynamodb",
  "description": "Typesafe DynamoDB DocumentClient type definitions for single-table designs",
  "version": "1.2.5-beta8",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": {
    "name": "Conor Brandon"
  },
  "homepage": "https://github.com/conorbrandon/ts-dynamodb",
  "bugs": {
    "url": "https://github.com/conorbrandon/ts-dynamodb/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/conorbrandon/ts-dynamodb"
  },
  "keywords": [
    "dynamodb",
    "typescript",
    "typesafe",
    "document client",
    "type",
    "aws"
  ],
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "package.json"
  ],
  "engines": {
    "node": ">=16.20.2"
  },
  "peerDependencies": {
    "aws-sdk": "^2",
    "typescript": "^5.3.2"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.506.0",
    "@aws-sdk/lib-dynamodb": "^3.506.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^16",
    "@typescript-eslint/eslint-plugin": "^6.20.0",
    "@typescript-eslint/parser": "^6.20.0",
    "@typescript/analyze-trace": "^0.10.1",
    "aws-sdk": "^2.1550.0",
    "eslint": "^8.56.0",
    "expect-type": "0.15.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2",
    "ts-loader": "^9.5.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "webpack": "^5.90.1",
    "webpack-cli": "^5.1.4",
    "webpack-node-externals": "^3.0.0",
    "zod": "^3.22.4"
  },
  "scripts": {
    "lint": "yarn eslint .",
    "build": "rm -rf ./dist && rm -rf ./.webpack && yarn lint && yarn tsc && yarn webpack -c ./webpack.config.js && yarn prettier --write --ignore-path '' --print-width 120 ./dist && cp ./.webpack/index.js ./dist",
    "test": "yarn jest --testPathIgnorePatterns \"no-unchecked\" && yarn jest \"no-unchecked\"",
    "ddb:create": "docker-compose -f ./tests/ddb-local/docker-compose.yml up -d && ts-node ./tests/ddb-local/create-tables-locally.ts",
    "ddb:down": "docker-compose -f ./tests/ddb-local/docker-compose.yml down",
    "ddb:reset": "yarn ddb:down && yarn ddb:create"
  },
  "jest": {
    "transform": {
      "^((?!no-unchecked-index-access).)*\\.ts$": [
        "ts-jest",
        {
          "tsconfig": "tests/tsconfig.json"
        }
      ],
      "^.*no-unchecked-index-access.*\\.ts$": [
        "ts-jest",
        {
          "tsconfig": "tests/no-unchecked-index-access/tsconfig.json"
        }
      ]
    }
  }
}